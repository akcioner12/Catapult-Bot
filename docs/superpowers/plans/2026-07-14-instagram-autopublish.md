# Instagram Autopublish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Every approved Telegram photo post and every approved YouTube Short also gets published to Instagram (feed photo / Reel respectively) through the same single approval action, with Instagram-specific SEO caption + hashtags generated by Claude — not the raw source text.

**Architecture:** Extract the Buffer GraphQL publish logic already proven for TikTok into a shared `buffer_publisher.py`, add a small Claude-backed caption/hashtag generator, and a thin `instagram_publisher.py` that wires the two together. Hook both into the existing publish flows (`tg_publisher.py::auto_publish` for photos, `yt_publisher.py::_finish_publish` for Reels) as best-effort, non-blocking, independently-retryable side effects — mirroring the TikTok precedent exactly, including its retry-pending-store pattern, fixed for a file-lifecycle bug that TikTok-only code doesn't hit but two independent retry consumers of the same video file would.

**Tech Stack:** Python 3, httpx (already a dependency), Buffer GraphQL API, Claude API (`claude-sonnet-4-6`, already used elsewhere in this codebase).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-14-instagram-autopublish-design.md` — every task below implements a section of it.
- No new pip dependencies — `httpx` is already in `requirements.txt` and used identically elsewhere.
- `BUFFER_INSTAGRAM_CHANNEL_ID=6a5600c480cc80cdcab03633` is already set on Railway (`Catapult-Bot` service only) — no config task needed.
- This codebase has no pytest suite (verified: no `tests/` dir, no pytest in `requirements.txt`, no existing `test_*.py` files). Established verification convention here is `python -m py_compile` for syntax + manual `python -c` scripts with asserts for logic, and live-tested manual verification only at explicit, clearly-flagged go/no-go points (see Task 8) — not automated unit tests. This plan follows that convention rather than introducing pytest unilaterally.
- Out of scope (per spec): Instagram Stories, Twitter/X, Buffer scheduled posting (`dueAt`) — always `mode: shareNow`.

---

### Task 1: `subagents/buffer_publisher.py` + shared media URL helper

**Files:**
- Create: `subagents/buffer_publisher.py`
- Modify: `subagents/media_push.py` (add a shared URL-building helper, currently duplicated inline in `tiktok_publisher.py`)

**Interfaces:**
- Produces: `publish_to_buffer(channel_id: str, caption: str, media_url: str, media_type: str) -> str | None` (`media_type` is `"video"` or `"image"`). `media_push.media_url(kind: str, local_path: str) -> str`.
- Consumes: nothing new — `BACKEND_URL`/`MEDIA_SERVE_TOKEN` already exist in `media_push.py`.

- [x] **Step 1: Add `media_url` to `subagents/media_push.py`**

Add at the end of the file (after `push_media`):

```python
def media_url(kind: str, local_path: str) -> str:
    filename = os.path.basename(local_path)
    return f"{BACKEND_URL}/media/{kind}/{filename}?token={MEDIA_SERVE_TOKEN}"
```

- [x] **Step 2: Create `subagents/buffer_publisher.py`**

```python
"""
Sub-agent: общая логика публикации через Buffer (используется и TikTok, и
Instagram паблишерами) — GraphQL createPost + поллинг статуса до sent/error.
"""
import asyncio
import logging
import os

import httpx

logger = logging.getLogger(__name__)

BUFFER_API_KEY = os.getenv("BUFFER_API_KEY", "")
BUFFER_URL = "https://api.buffer.com"

CREATE_POST_MUTATION = """
mutation($input: CreatePostInput!) {
  createPost(input: $input) {
    ... on PostActionSuccess { post { id status } }
    ... on InvalidInputError { message }
    ... on UnauthorizedError { message }
    ... on UnexpectedError { message }
    ... on LimitReachedError { message }
    ... on NotFoundError { message }
    ... on RestProxyError { message }
  }
}
"""

POST_STATUS_QUERY = """
query($id: PostId!) {
  post(input: {id: $id}) { id status sentAt externalLink }
}
"""


async def publish_to_buffer(channel_id: str, caption: str, media_url: str, media_type: str) -> str | None:
    """Публикует media_url (уже публичный HTTPS-адрес) в канал channel_id через
    Buffer. media_type: "video" или "image". Возвращает ссылку на пост, или
    None при ошибке/таймауте (никогда не бросает исключение)."""
    if not BUFFER_API_KEY or not channel_id:
        logger.warning("BUFFER_API_KEY/channel_id не заданы — пропускаем публикацию через Buffer")
        return None

    headers = {"Authorization": f"Bearer {BUFFER_API_KEY}", "Content-Type": "application/json"}

    try:
        async with httpx.AsyncClient(timeout=60) as client:
            resp = await client.post(
                BUFFER_URL,
                headers=headers,
                json={
                    "query": CREATE_POST_MUTATION,
                    "variables": {
                        "input": {
                            "channelId": channel_id,
                            "text": caption[:2200],
                            "mode": "shareNow",
                            "schedulingType": "automatic",
                            "assets": [{media_type: {"url": media_url}}],
                        }
                    },
                },
            )
            data = resp.json()
            result = data.get("data", {}).get("createPost", {})
            if "message" in result:
                logger.error(f"Buffer createPost error: {result['message']}")
                return None
            post_id = result.get("post", {}).get("id")
            if not post_id:
                logger.error(f"Buffer createPost: неожиданный ответ: {data}")
                return None

            for _ in range(24):  # до ~2 минут ожидания публикации
                await asyncio.sleep(5)
                status_resp = await client.post(
                    BUFFER_URL,
                    headers=headers,
                    json={"query": POST_STATUS_QUERY, "variables": {"id": post_id}},
                )
                post = status_resp.json().get("data", {}).get("post", {})
                if post.get("status") == "sent":
                    logger.info(f"✅ Опубликовано через Buffer: {post['externalLink']}")
                    return post["externalLink"]
                if post.get("status") == "error":
                    logger.error(f"Buffer: публикация завершилась ошибкой: {post}")
                    return None

            logger.error("Buffer: публикация не завершилась за отведённое время")
            return None
    except Exception as e:
        logger.error(f"publish_to_buffer error: {e}")
        return None
```

- [x] **Step 3: Verify syntax**

Run: `cd catapult-bot-git && python -m py_compile subagents/buffer_publisher.py subagents/media_push.py`
Expected: no output, exit code 0.

- [x] **Step 4: Manual verification (shape only, no live post)**

```bash
python -c "
from subagents.buffer_publisher import publish_to_buffer
from subagents.media_push import media_url
import asyncio
# No BUFFER_API_KEY set in this shell -> must skip gracefully, not raise.
result = asyncio.run(publish_to_buffer('some_channel', 'caption', 'https://example.com/x.jpg', 'image'))
assert result is None, result
print(media_url('photos', '/data/photos/test.jpg'))
print('OK')
"
```

Expected: prints the built URL (`.../media/photos/test.jpg?token=`), then `OK` — confirms the missing-config path returns `None` without raising, and `media_url` builds the expected shape. No real Buffer call is made (no channel exists named `'some_channel'`, and if `BUFFER_API_KEY` happens to be set in this shell, this assert would fail loudly rather than silently posting anything — nothing is published either way since `'some_channel'` isn't a real channel id).

- [x] **Step 5: Commit**

```bash
git add subagents/buffer_publisher.py subagents/media_push.py
git commit -m "feat: extract shared Buffer publish helper from tiktok_publisher"
```

---

### Task 2: Refactor `tiktok_publisher.py` onto `buffer_publisher`

**Files:**
- Modify: `subagents/tiktok_publisher.py` (full rewrite — same external interface)

**Interfaces:**
- Consumes: `buffer_publisher.publish_to_buffer` (Task 1), `media_push.push_media`/`media_url`.
- Produces: `upload_to_tiktok(video_path: str, caption: str) -> str | None` — **unchanged signature**, so `yt_publisher.py`'s existing `from subagents.tiktok_publisher import upload_to_tiktok` and all its call sites need no changes.

- [x] **Step 1: Replace `subagents/tiktok_publisher.py` contents**

```python
"""
Sub-agent: публикация видео в TikTok через Buffer (уже прошедший аудит
TikTok Content Posting API — без него загрузка через официальный API TikTok
была бы видна только самому аккаунту, не подписчикам).
"""
import os
import logging

from subagents.buffer_publisher import publish_to_buffer
from subagents.media_push import push_media, media_url

logger = logging.getLogger(__name__)

BUFFER_TIKTOK_CHANNEL_ID = os.getenv("BUFFER_TIKTOK_CHANNEL_ID", "")


async def upload_to_tiktok(video_path: str, caption: str) -> str | None:
    """Публикует video_path в TikTok через Buffer. Возвращает ссылку на пост или None."""
    if not BUFFER_TIKTOK_CHANNEL_ID:
        logger.warning("BUFFER_TIKTOK_CHANNEL_ID не задан — пропускаем публикацию в TikTok")
        return None
    if not os.path.exists(video_path):
        logger.error(f"upload_to_tiktok: файл не найден {video_path}")
        return None
    if not await push_media("videos", video_path):
        logger.error("upload_to_tiktok: не удалось выложить видео на web")
        return None

    return await publish_to_buffer(BUFFER_TIKTOK_CHANNEL_ID, caption, media_url("videos", video_path), "video")
```

- [x] **Step 2: Verify syntax and unchanged interface**

```bash
cd catapult-bot-git && python -m py_compile subagents/tiktok_publisher.py
python -c "
import inspect
from subagents.tiktok_publisher import upload_to_tiktok
sig = str(inspect.signature(upload_to_tiktok))
assert sig == '(video_path: str, caption: str) -> str | None', sig
print('OK')
"
```

Expected: `OK` — confirms the function signature callers depend on (`yt_publisher.py`) is byte-for-byte unchanged.

**Not live-tested here on purpose:** re-running a real TikTok post just to verify this mechanical refactor would spam the live `crypto_ai_forex` TikTok channel for no new behavior. Task 8's end-to-end pass exercises this same code path for real when it verifies Reels, so an equivalence check (Step 2 above) plus a read-through diff against the pre-refactor version is sufficient here — the diff shows the same three guard checks in the same order (`BUFFER_TIKTOK_CHANNEL_ID` → file exists → push succeeds), same GraphQL mutation/polling behavior (now inside `publish_to_buffer`, called with the same arguments in the same order), no logic removed or reordered.

- [x] **Step 3: Commit**

```bash
git add subagents/tiktok_publisher.py
git commit -m "refactor: route tiktok_publisher through the shared buffer_publisher"
```

---

### Task 3: `subagents/instagram_caption.py`

**Files:**
- Create: `subagents/instagram_caption.py`

**Interfaces:**
- Consumes: `CLAUDE_API_KEY`, `CLAUDE_API_URL` from `subagents.rewriter` (already used by `image_brief.py`/`yt_script.py` the same way).
- Produces: `generate_instagram_caption(source_text: str, category: str, content_type: str) -> dict` — always returns `{"caption": str, "hashtags": list[str]}`, never raises. `content_type` is `"photo"` or `"reel"`.

- [x] **Step 1: Create `subagents/instagram_caption.py`**

```python
"""
Sub-agent: SEO-подпись и хэштеги для Instagram (лента и Reels) через Claude.
"""
import re
import logging

import httpx

from subagents.rewriter import CLAUDE_API_KEY, CLAUDE_API_URL

logger = logging.getLogger(__name__)

CONTENT_TYPE_STYLE = {
    "photo": "чуть длиннее, можно 2-4 предложения, разговорный тон",
    "reel": "коротко и энергично, 1-2 предложения, крючок в первой строке",
}


async def generate_instagram_caption(source_text: str, category: str, content_type: str) -> dict:
    """content_type: "photo" или "reel". Возвращает {"caption": str, "hashtags": list[str]}.
    При сбое Claude или парсинга — возвращает исходный текст как caption и
    пустой список хэштегов (публикация всё равно пройдёт, просто без SEO)."""
    style = CONTENT_TYPE_STYLE.get(content_type, CONTENT_TYPE_STYLE["photo"])
    prompt = f"""Ты ведёшь Instagram-аккаунт {category}-тематики (крипта/финансы/ИИ) на русском языке.

Исходный текст:
{source_text[:1500]}

Напиши подпись для Instagram под этот контент — {style}. Без эмодзи через каждое слово, без markdown, без заголовков.
Подбери 10-15 хэштегов: смесь широких (#crypto, #bitcoin), нишевых по теме поста и брендовых (#catapulttrade).

Ответь СТРОГО в этом формате, без пояснений:
CAPTION:
<текст подписи>
HASHTAGS: #tag1 #tag2 #tag3 ..."""

    try:
        async with httpx.AsyncClient(timeout=20) as client:
            resp = await client.post(
                CLAUDE_API_URL,
                headers={
                    "x-api-key": CLAUDE_API_KEY,
                    "anthropic-version": "2023-06-01",
                    "content-type": "application/json",
                },
                json={
                    "model": "claude-sonnet-4-6",
                    "max_tokens": 400,
                    "messages": [{"role": "user", "content": prompt}],
                },
            )
            data = resp.json()
            if "content" not in data:
                logger.error(f"Instagram caption Claude error: {data}")
                return {"caption": source_text[:2000], "hashtags": []}
            raw = data["content"][0]["text"]
    except Exception as e:
        logger.error(f"generate_instagram_caption error: {e}")
        return {"caption": source_text[:2000], "hashtags": []}

    return _parse_caption(raw, source_text)


def _parse_caption(raw: str, fallback_text: str) -> dict:
    caption_match = re.search(r"CAPTION:\s*(.+?)(?=\nHASHTAGS:|\Z)", raw, re.DOTALL)
    hashtags_match = re.search(r"HASHTAGS:\s*(.+)", raw)
    if not caption_match:
        logger.error(f"Не удалось распарсить подпись Instagram: {raw[:300]}")
        return {"caption": fallback_text[:2000], "hashtags": []}
    caption = caption_match.group(1).strip()
    hashtags = re.findall(r"#\w+", hashtags_match.group(1)) if hashtags_match else []
    return {"caption": caption, "hashtags": hashtags}
```

- [x] **Step 2: Verify syntax**

Run: `cd catapult-bot-git && python -m py_compile subagents/instagram_caption.py`
Expected: no output, exit code 0.

- [x] **Step 3: Unit-level check of the parser (no network)**

```bash
python -c "
from subagents.instagram_caption import _parse_caption
raw = 'CAPTION:\nBitcoin just broke a key level — here is what it means for you.\nHASHTAGS: #crypto #bitcoin #catapulttrade #trading'
result = _parse_caption(raw, 'fallback')
assert result['caption'] == 'Bitcoin just broke a key level — here is what it means for you.', result
assert result['hashtags'] == ['#crypto', '#bitcoin', '#catapulttrade', '#trading'], result
# malformed input falls back cleanly
bad = _parse_caption('garbage, no labels', 'fallback text')
assert bad == {'caption': 'fallback text', 'hashtags': []}, bad
print('OK')
"
```

Expected: `OK`.

- [x] **Step 4: Manual verification — real Claude call**

`CLAUDE_API_KEY` is already configured in this environment (used by `image_brief.py` etc.), so this call is real but harmless — it only generates text, nothing gets posted anywhere:

```bash
python -c "
import asyncio
from subagents.instagram_caption import generate_instagram_caption
result = asyncio.run(generate_instagram_caption(
    'Bitcoin пробил отметку \$70000 впервые за три месяца на фоне роста институционального спроса.',
    'crypto', 'photo'
))
print(result)
assert result['caption'], result
assert len(result['hashtags']) > 0, result
print('OK')
"
```

Expected: prints a dict with a real generated caption and a non-empty hashtag list, then `OK`.

- [x] **Step 5: Commit**

```bash
git add subagents/instagram_caption.py
git commit -m "feat: add generate_instagram_caption for Instagram SEO captions/hashtags"
```

---

### Task 4: `subagents/instagram_publisher.py`

**Files:**
- Create: `subagents/instagram_publisher.py`

**Interfaces:**
- Consumes: `buffer_publisher.publish_to_buffer` (Task 1), `media_push.push_media`/`media_url`, `instagram_caption.generate_instagram_caption` (Task 3).
- Produces: `upload_photo_to_instagram(photo_path: str, source_text: str, category: str) -> str | None`, `upload_reel_to_instagram(video_path: str, source_text: str, category: str) -> str | None`.

- [x] **Step 1: Create `subagents/instagram_publisher.py`**

```python
"""
Sub-agent: публикация фото-постов и Reels в Instagram через Buffer.
"""
import os
import logging

from subagents.buffer_publisher import publish_to_buffer
from subagents.media_push import push_media, media_url
from subagents.instagram_caption import generate_instagram_caption

logger = logging.getLogger(__name__)

BUFFER_INSTAGRAM_CHANNEL_ID = os.getenv("BUFFER_INSTAGRAM_CHANNEL_ID", "")


def _full_caption(parts: dict) -> str:
    if parts["hashtags"]:
        return f"{parts['caption']}\n\n{' '.join(parts['hashtags'])}"
    return parts["caption"]


async def upload_photo_to_instagram(photo_path: str, source_text: str, category: str) -> str | None:
    """Публикует photo_path в Instagram (лента) через Buffer. Файл уже должен
    быть на web (generate_image пушит его при генерации, image_generator.py) —
    не пушится повторно здесь. Возвращает ссылку на пост или None."""
    if not BUFFER_INSTAGRAM_CHANNEL_ID:
        logger.warning("BUFFER_INSTAGRAM_CHANNEL_ID не задан — пропускаем публикацию в Instagram")
        return None
    if not os.path.exists(photo_path):
        logger.error(f"upload_photo_to_instagram: файл не найден {photo_path}")
        return None

    parts = await generate_instagram_caption(source_text, category, "photo")
    return await publish_to_buffer(
        BUFFER_INSTAGRAM_CHANNEL_ID, _full_caption(parts), media_url("photos", photo_path), "image"
    )


async def upload_reel_to_instagram(video_path: str, source_text: str, category: str) -> str | None:
    """Публикует video_path в Instagram Reels через Buffer. Возвращает ссылку
    на пост или None."""
    if not BUFFER_INSTAGRAM_CHANNEL_ID:
        logger.warning("BUFFER_INSTAGRAM_CHANNEL_ID не задан — пропускаем публикацию в Instagram")
        return None
    if not os.path.exists(video_path):
        logger.error(f"upload_reel_to_instagram: файл не найден {video_path}")
        return None
    if not await push_media("videos", video_path):
        logger.error("upload_reel_to_instagram: не удалось выложить видео на web")
        return None

    parts = await generate_instagram_caption(source_text, category, "reel")
    return await publish_to_buffer(
        BUFFER_INSTAGRAM_CHANNEL_ID, _full_caption(parts), media_url("videos", video_path), "video"
    )
```

- [x] **Step 2: Verify syntax**

Run: `cd catapult-bot-git && python -m py_compile subagents/instagram_publisher.py`
Expected: no output, exit code 0.

- [x] **Step 3: Manual verification (import + missing-file guard, no live post)**

```bash
python -c "
import asyncio
from subagents.instagram_publisher import upload_photo_to_instagram, upload_reel_to_instagram
# nonexistent path -> must return None, not raise, even with real config present
assert asyncio.run(upload_photo_to_instagram('/data/photos/does_not_exist.jpg', 'text', 'crypto')) is None
assert asyncio.run(upload_reel_to_instagram('/data/videos/does_not_exist.mp4', 'text', 'crypto')) is None
print('OK')
"
```

Expected: `OK`. This deliberately does not exercise a real Buffer post — that happens once, end-to-end, in Task 8, after this module is actually wired into the publish flows (Tasks 5-6) so the whole chain gets validated together instead of in an artificial standalone call.

- [x] **Step 4: Commit**

```bash
git add subagents/instagram_publisher.py
git commit -m "feat: add instagram_publisher for photo/Reel publishing via Buffer"
```

---

### Task 5: Wire photo publishing into `tg_publisher.py::auto_publish`

**Files:**
- Modify: `subagents/tg_publisher.py:22` (imports), `subagents/tg_publisher.py:709-720` (poll branch), `subagents/tg_publisher.py:730-746` (regular branch)

**Interfaces:**
- Consumes: `instagram_publisher.upload_photo_to_instagram` (Task 4).

- [x] **Step 1: Add the import**

In `subagents/tg_publisher.py`, after line 22 (`from subagents.rewriter import generate_catapult_post, generate_post_claude, CATAPULT_ANGLES`), add:

```python
from subagents.instagram_publisher import upload_photo_to_instagram
```

- [x] **Step 2: Wire the poll branch**

Find (around line 709-720):

```python
                if post.get("photo_path") and os.path.exists(post["photo_path"]):
                    from telegram import Bot, InputFile
                    main_bot = Bot(token=MAIN_BOT_TOKEN)
                    try:
                        with open(post["photo_path"], "rb") as photo_file:
                            await main_bot.send_photo(chat_id=CHANNEL_ID, photo=InputFile(photo_file))
                        try:
                            os.remove(post["photo_path"])
                        except Exception:
                            pass
                    except Exception as photo_err:
                        logger.warning(f"poll sendPhoto failed: {photo_err}")
```

Replace with:

```python
                if post.get("photo_path") and os.path.exists(post["photo_path"]):
                    from telegram import Bot, InputFile
                    main_bot = Bot(token=MAIN_BOT_TOKEN)
                    try:
                        with open(post["photo_path"], "rb") as photo_file:
                            await main_bot.send_photo(chat_id=CHANNEL_ID, photo=InputFile(photo_file))
                        instagram_url = await upload_photo_to_instagram(post["photo_path"], post["text"], category)
                        if instagram_url:
                            logger.info(f"✅ Опубликовано в Instagram: {instagram_url}")
                        else:
                            await client.post(
                                f"https://api.telegram.org/bot{PARSER_BOT_TOKEN}/sendMessage",
                                json={
                                    "chat_id": ADMIN_TG_ID,
                                    "text": "⚠️ Не удалось опубликовать фото-пост в Instagram.",
                                    "parse_mode": "HTML",
                                },
                            )
                        try:
                            os.remove(post["photo_path"])
                        except Exception:
                            pass
                    except Exception as photo_err:
                        logger.warning(f"poll sendPhoto failed: {photo_err}")
```

- [x] **Step 3: Wire the regular (non-poll) branch**

Find (around line 730-746):

```python
            elif post.get("photo_path") and os.path.exists(post["photo_path"]):
                # Сначала фото без текста, потом полный текст с футером
                from telegram import Bot, InputFile
                main_bot = Bot(token=MAIN_BOT_TOKEN)
                try:
                    with open(post["photo_path"], "rb") as photo_file:
                        await main_bot.send_photo(
                            chat_id=CHANNEL_ID,
                            photo=InputFile(photo_file),
                        )
                    logger.info(f"✅ Фото опубликовано")
                    try:
                        os.remove(post["photo_path"])
                    except Exception:
                        pass
                except Exception as photo_err:
                    logger.warning(f"sendPhoto failed: {photo_err}, публикую без картинки")
```

Replace with:

```python
            elif post.get("photo_path") and os.path.exists(post["photo_path"]):
                # Сначала фото без текста, потом полный текст с футером
                from telegram import Bot, InputFile
                main_bot = Bot(token=MAIN_BOT_TOKEN)
                try:
                    with open(post["photo_path"], "rb") as photo_file:
                        await main_bot.send_photo(
                            chat_id=CHANNEL_ID,
                            photo=InputFile(photo_file),
                        )
                    logger.info(f"✅ Фото опубликовано")
                    instagram_url = await upload_photo_to_instagram(post["photo_path"], post["text"], category)
                    if instagram_url:
                        logger.info(f"✅ Опубликовано в Instagram: {instagram_url}")
                    else:
                        await client.post(
                            f"https://api.telegram.org/bot{PARSER_BOT_TOKEN}/sendMessage",
                            json={
                                "chat_id": ADMIN_TG_ID,
                                "text": "⚠️ Не удалось опубликовать фото-пост в Instagram.",
                                "parse_mode": "HTML",
                            },
                        )
                    try:
                        os.remove(post["photo_path"])
                    except Exception:
                        pass
                except Exception as photo_err:
                    logger.warning(f"sendPhoto failed: {photo_err}, публикую без картинки")
```

- [x] **Step 4: Verify syntax**

Run: `cd catapult-bot-git && python -m py_compile subagents/tg_publisher.py`
Expected: no output, exit code 0.

- [x] **Step 5: Manual verification — confirm both edit sites landed correctly**

```bash
python -c "
import ast
tree = ast.parse(open('subagents/tg_publisher.py', encoding='utf-8').read())
src = open('subagents/tg_publisher.py', encoding='utf-8').read()
assert src.count('upload_photo_to_instagram(post[\"photo_path\"], post[\"text\"], category)') == 2, 'expected exactly 2 call sites (poll + regular branch)'
print('OK')
"
```

Expected: `OK` — confirms the call was added to both branches and the file still parses as valid Python.

- [x] **Step 6: Commit**

```bash
git add subagents/tg_publisher.py
git commit -m "feat: publish approved photo posts to Instagram alongside Telegram"
```

---

### Task 6: Wire Reels into `yt_publisher.py::_finish_publish` + fix cross-platform file lifecycle

**Files:**
- Modify: `subagents/yt_publisher.py:19-23` (imports), `subagents/yt_publisher.py:34` (file constant), `subagents/yt_publisher.py:74` (dict), `subagents/yt_publisher.py:124-130` (save function), `subagents/yt_publisher.py:154-160` (load logic), `subagents/yt_publisher.py:462-496` (`_finish_publish`), `subagents/yt_publisher.py:532-544` (`retry_tiktok_upload` + new `retry_instagram_upload`)

**Interfaces:**
- Consumes: `instagram_publisher.upload_reel_to_instagram` (Task 4).
- Produces: `instagram_retry_pending: dict`, `save_instagram_retry_pending()`, `retry_instagram_upload(video_id: str)`.

- [x] **Step 1: Add the import**

In `subagents/yt_publisher.py`, line 19 currently reads:

```python
from subagents.tiktok_publisher import upload_to_tiktok
```

Add directly below it:

```python
from subagents.instagram_publisher import upload_reel_to_instagram
```

- [x] **Step 2: Add the retry-pending file constant and dict**

Line 34 currently reads `TIKTOK_RETRY_FILE = "/data/tiktok_retry_pending.json"` — add directly below it:

```python
INSTAGRAM_RETRY_FILE = "/data/instagram_retry_pending.json"
```

Line 74 currently reads `tiktok_retry_pending: dict = {}` — add directly below it:

```python
instagram_retry_pending: dict = {}
```

- [x] **Step 3: Add `save_instagram_retry_pending`**

After `save_tiktok_retry_pending` (lines 124-129), add:

```python
def save_instagram_retry_pending():
    try:
        with open(INSTAGRAM_RETRY_FILE, "w", encoding="utf-8") as f:
            json.dump(instagram_retry_pending, f, ensure_ascii=False, default=str)
    except Exception as e:
        logger.error(f"Save instagram retry pending error: {e}")
```

- [x] **Step 4: Load it at startup**

Inside `load_pending_videos()`, after the `TIKTOK_RETRY_FILE` block (lines 154-160) and before the `FAILED_UPLOADS_FILE` block, add:

```python
    try:
        if os.path.exists(INSTAGRAM_RETRY_FILE):
            with open(INSTAGRAM_RETRY_FILE, "r", encoding="utf-8") as f:
                instagram_retry_pending.update(json.load(f))
            logger.info(f"Загружено {len(instagram_retry_pending)} видео на повтор Instagram")
    except Exception as e:
        logger.error(f"Load instagram retry pending error: {e}")
```

- [x] **Step 5: Rewrite `_finish_publish`**

Replace the whole function (lines 462-496):

```python
async def _finish_publish(video_id: str, video: dict, youtube_id: str):
    """После успешной загрузки на YouTube: анонсирует в канале, пробует TikTok
    (с одной попыткой более лояльного fallback-варианта при блоке) и Instagram
    Reels, и шлёт админу сводку по всем площадкам. Общий код для первого
    одобрения, /retry_videos и запланированной публикации."""
    await announce_in_telegram(youtube_id, video["title"], video.get("thumbnail_path"))

    status_lines = [f"✅ YouTube: https://youtu.be/{youtube_id}"]
    block_reason = await check_tiktok_compliance(_tiktok_caption(video))

    if block_reason:
        fallback_result = await _attempt_tiktok_fallback(video_id, video, block_reason) if block_reason != FAIL_CLOSED_REASON else None
        if fallback_result:
            tiktok_url, note = fallback_result
            status_lines.append(f"✅ TikTok: {tiktok_url} ({note})")
        else:
            status_lines.append(f"⚠️ TikTok пропущен: {block_reason}")
    else:
        tiktok_url = await upload_to_tiktok(video["video_path"], _tiktok_caption(video))
        if tiktok_url:
            status_lines.append(f"✅ TikTok: {tiktok_url}")
        else:
            status_lines.append("⚠️ TikTok не удался — /retry_tiktok")
            tiktok_retry_pending[video_id] = video
            save_tiktok_retry_pending()

    instagram_url = await upload_reel_to_instagram(video["video_path"], _tiktok_caption(video), video["category"])
    if instagram_url:
        status_lines.append(f"✅ Instagram: {instagram_url}")
    else:
        status_lines.append("⚠️ Instagram не удался — /retry_instagram")
        instagram_retry_pending[video_id] = video
        save_instagram_retry_pending()

    if video_id not in tiktok_retry_pending and video_id not in instagram_retry_pending:
        try:
            os.remove(video["video_path"])
        except Exception:
            pass

    await notify_admin("<b>Видео опубликовано:</b>\n" + "\n".join(status_lines))
```

This replaces the two scattered `os.remove(video["video_path"])` calls that used to live inside each branch (one unconditional in the `block_reason` branch, one gated on `tiktok_url` in the `else` branch) with a single delete at the end, gated on *neither* platform having a pending retry — see the design spec's "File-lifecycle fix" section for why: the old per-branch deletes were correct when TikTok was the only retry-capable consumer of `video["video_path"]`, but a second one (Instagram) means whichever platform's attempt finishes must not delete a file the other still needs.

- [x] **Step 6: Fix `retry_tiktok_upload` and add `retry_instagram_upload`**

Replace `retry_tiktok_upload` (lines 532-544):

```python
async def retry_tiktok_upload(video_id: str):
    video = tiktok_retry_pending.get(video_id)
    if not video:
        return
    tiktok_url = await upload_to_tiktok(video["video_path"], _tiktok_caption(video))
    if not tiktok_url:
        return
    tiktok_retry_pending.pop(video_id, None)
    save_tiktok_retry_pending()
    if video_id not in instagram_retry_pending:
        try:
            os.remove(video["video_path"])
        except Exception:
            pass

async def retry_instagram_upload(video_id: str):
    video = instagram_retry_pending.get(video_id)
    if not video:
        return
    instagram_url = await upload_reel_to_instagram(video["video_path"], _tiktok_caption(video), video["category"])
    if not instagram_url:
        return
    instagram_retry_pending.pop(video_id, None)
    save_instagram_retry_pending()
    if video_id not in tiktok_retry_pending:
        try:
            os.remove(video["video_path"])
        except Exception:
            pass
```

- [x] **Step 7: Verify syntax**

Run: `cd catapult-bot-git && python -m py_compile subagents/yt_publisher.py`
Expected: no output, exit code 0.

- [x] **Step 8: Manual verification — file-lifecycle guard logic in isolation**

This is the bug found during the spec's self-review — worth a dedicated check that doesn't depend on any network call:

```bash
python -c "
import subagents.yt_publisher as yp

# Simulate: both platforms failed and are pending retry for the same video_id.
video_id = 'test123'
yp.tiktok_retry_pending[video_id] = {'video_path': '/tmp/does_not_matter.mp4', 'category': 'crypto', 'title': 't', 'description': 'd'}
yp.instagram_retry_pending[video_id] = yp.tiktok_retry_pending[video_id]

# _finish_publish's own delete condition (mirrored here, not re-running the whole function):
should_delete = video_id not in yp.tiktok_retry_pending and video_id not in yp.instagram_retry_pending
assert should_delete is False, 'must NOT delete while either platform still has a pending retry'

# Simulate TikTok's retry succeeding first (as retry_tiktok_upload would, without the network call):
yp.tiktok_retry_pending.pop(video_id, None)
should_delete_after_tiktok_retry = video_id not in yp.instagram_retry_pending
assert should_delete_after_tiktok_retry is False, 'Instagram retry is still pending -- file must survive'

# Now Instagram's retry succeeds too:
yp.instagram_retry_pending.pop(video_id, None)
should_delete_after_both = video_id not in yp.tiktok_retry_pending
assert should_delete_after_both is True, 'both resolved -- now safe to delete'

print('OK')
"
```

Expected: `OK` — proves the guard logic added in Steps 5-6 actually prevents the premature-delete bug the self-review found, without needing a real video file or any network call.

- [x] **Step 9: Commit**

```bash
git add subagents/yt_publisher.py
git commit -m "feat: publish approved Reels to Instagram, fix shared video-file retry lifecycle"
```

---

### Task 7: `/retry_instagram` admin command

**Files:**
- Modify: `parser.py` (import, new command function, handler registration)

**Interfaces:**
- Consumes: `yt_publisher.instagram_retry_pending`, `yt_publisher.retry_instagram_upload` (Task 6).

- [x] **Step 1: Extend the existing `yt_publisher` import**

`parser.py` already has (per the current `feature/youtube-shorts-pipeline` merge):

```python
from subagents.yt_publisher import (
    pending_videos, approved_videos, awaiting_self_record_video, tiktok_retry_pending,
    save_pending_videos, load_pending_videos, handle_video_approval, handle_video_file,
)
```

Add `instagram_retry_pending` to that tuple, so it reads:

```python
from subagents.yt_publisher import (
    pending_videos, approved_videos, awaiting_self_record_video, tiktok_retry_pending,
    instagram_retry_pending, save_pending_videos, load_pending_videos, handle_video_approval,
    handle_video_file,
)
```

(Exact current import block may have additional names from other work landed on `main` since — add `instagram_retry_pending` to whatever the block looks like at implementation time; don't remove anything already there.)

- [x] **Step 2: Add `cmd_retry_instagram`**

Directly after `cmd_retry_tiktok` (currently at parser.py lines 127-135), add:

```python
async def cmd_retry_instagram(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if update.effective_user.id != ADMIN_TG_ID:
        return
    if not instagram_retry_pending:
        await update.message.reply_text("📭 Нет видео для повторной публикации в Instagram.")
        return
    await update.message.reply_text(f"🔄 Повторная публикация {len(instagram_retry_pending)} видео в Instagram...")
    for video_id in list(instagram_retry_pending.keys()):
        await yt_publisher.retry_instagram_upload(video_id)
```

- [x] **Step 3: Register the command handler**

Find `parser_app.add_handler(CommandHandler("retry_tiktok", cmd_retry_tiktok))` (currently line 1137) and add directly below it:

```python
    parser_app.add_handler(CommandHandler("retry_instagram", cmd_retry_instagram))
```

- [x] **Step 4: Verify syntax**

Run: `cd catapult-bot-git && python -m py_compile parser.py`
Expected: no output, exit code 0.

- [x] **Step 5: Manual verification**

```bash
python -c "
import ast
src = open('parser.py', encoding='utf-8').read()
assert 'cmd_retry_instagram' in src
assert 'instagram_retry_pending' in src
assert '\"retry_instagram\"' in src
ast.parse(src)
print('OK')
"
```

Expected: `OK`.

- [x] **Step 6: Commit**

```bash
git add parser.py
git commit -m "feat: add /retry_instagram admin command"
```

---

### Task 8: End-to-end manual verification (live — requires explicit go-ahead)

**Files:** none (verification only)

This is the point analogous to the YouTube Shorts pipeline's own Task 14 — the first real posts to the live `@crypto.ai.forex` Instagram account happen here, not before. Confirm with the user before running Steps 2-3.

- [x] **Step 1: Deploy**

Push the branch to `main` (or merge if working on a feature branch) and let Railway deploy `Catapult-Bot`. Stop `exciting-patience` afterward (it wakes on every push and conflicts with the bot's `getUpdates` — same as every other deploy in this project).

- [x] **Step 2: Verify a real photo post**

Trigger a normal photo-post publish (either wait for the next scheduled slot, or use `/test_publish` if a post is already approved and queued). Confirm:
- The photo appears on Telegram as before (unaffected).
- Within a minute or two, the same photo appears on `instagram.com/crypto.ai.forex` as a feed post, with a Claude-generated caption + hashtags (not the raw Telegram post text verbatim).
- This is also the first live confirmation of the `assets: [{"image": {...}}]` shape the spec flagged as unverified — if Buffer rejects it, check the error against Buffer's actual schema (introspect `CreatePostInput`'s `assets` field type) and adjust `buffer_publisher.py`'s asset-building accordingly.

- [x] **Step 3: Verify a real Reel**

Trigger `/generate_video`, approve the resulting video. Confirm:
- YouTube and TikTok publish as before (unaffected — Task 2's refactor gets its real live confirmation here too).
- Within a couple minutes, the same video appears as a Reel on `instagram.com/crypto.ai.forex`, with its own Claude-generated caption.

- [x] **Step 4: Verify the failure/retry path**

Temporarily set `BUFFER_INSTAGRAM_CHANNEL_ID` to an invalid value on Railway, trigger one more photo post and one more video, confirm:
- Both still publish successfully everywhere else (Telegram/YouTube/TikTok) — Instagram is skipped with a warning, admin gets the "⚠️ Не удалось опубликовать фото-пост в Instagram" message for the photo, and `/retry_instagram` reports the pending video by name for the Reel.
- Restore the real `BUFFER_INSTAGRAM_CHANNEL_ID` value, run `/retry_instagram`, confirm the pending Reel now publishes.

- [x] **Step 5: Confirm with the user**

Report back what was verified (with actual Instagram post links) before considering this plan complete.
